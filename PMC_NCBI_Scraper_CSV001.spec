# -*- mode: python ; coding: utf-8 -*-


block_cipher = None


a = Analysis(
    ['main.py'],
    pathex=[],
    binaries=[],
    datas=[('PMC_New_Edit\\items.py','.'),
    ('PMC_New_Edit\\middlewares.py','.'),
    ('PMC_New_Edit\\pipelines.py','.'),
    ('PMC_New_Edit\\settings.py','.'),
    ('PMC_New_Edit\\spiders','spiders'),
    ('scrapy.cfg','.'),
    ('kill.bat','.'),
    ('expresso.exe','.'),
    ('Includes\\Application','Includes\\Application')],
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    win_no_prefer_redirects=False,
    win_private_assemblies=False,
    cipher=block_cipher,
    noarchive=False,
)
pyz = PYZ(a.pure, a.zipped_data, cipher=block_cipher)

exe = EXE(
    pyz,
    a.scripts,
    [],
    exclude_binaries=True,
    name='PMC_NCBI_test',
    debug=True,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    console=True,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
)
coll = COLLECT(
    exe,
    a.binaries,
    a.zipfiles,
    a.datas,
    strip=False,
    upx=True,
    upx_exclude=[],
    name='PMC_NCBI_test',
)
